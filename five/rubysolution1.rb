# @param {String} s
# @return {String}
def longest_palindrome(s)

    backwardsCounter = 0
    current = Array.new

    # Iterate through array of all characters in string
    for i in s.chars

        # Check if character has occurred yet
        if current.include?(i)

            # Check if duplicate is one position away or two


        end
    end
end
